== README

First attempt at Critiq

Databases so far:
  Users:
    (_) user_id
    (_) products (set of product_ids)
    (_) image-links (store in /images/users/{id}/filename)
    (x) name
    (x) link
    (x) age
  
  Products:
    (_) product_id
    (_) image-link (store in /images/products/{id}/filename)
    (_) features (set of featur_ids)
    (x) name
    (x) description


Still Needed
  Features:
    (_) feature_id
    (_) name
    (_) description to vote_count mapping

