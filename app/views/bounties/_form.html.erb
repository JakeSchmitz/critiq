<%= nested_form_for [@product, @bounty] do |f| %>
  <% if @bounty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bounty.errors.count, "error") %> prohibited this bounty from being saved:</h2>
      <ul>
      <% @product.bounties.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="bounty-form">
    <%= f.text_area :question, :placeholder => "Enter Bounty Question Here!", :maxlength => 1000, style: 'width: 600px; height: 100px; border-radius: 0px;' %>
  </div>
  <div class="actions">
    <%= f.submit nil, :class => 'btn btn-primary pull-right' %>
  </div>
<% end %>