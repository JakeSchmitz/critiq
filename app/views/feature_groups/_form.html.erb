<%= nested_form_for [@product, @feature_group], :html => { :class => 'form-horizontal', :multipart => true } do |f| %>
  <% if @feature_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feature_group.errors.count, "error") %> prohibited this feature group from being saved:</h2>

      <ul>
      <% @feature_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h4>Comparison Title</h4>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit nil, :class => 'btn btn-primary'%>
  </div>
<% end %>
