<div class="comment-box">
  <%= nested_form_for [@product, @comment] do |f| %>
    <% if @comment.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
        <ul>
        <% @product.comments.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div class="comment-form">
      <%= f.text_area :body, :placeholder => "Enter a Critiq Here", :maxlength => 1000, style: 'width: 500px; height: 100px; margin-left: 100px; margin-top: -105px; border-radius: 0px;' %>
    </div>
    <div class="actions">
      <%= f.submit nil, :class => 'btn btn-primary pull-right' %>
    </div>
  <% end %>
</div>
